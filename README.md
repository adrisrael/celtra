# Celtra

A cinematic, single-page marketplace concept for creators, built from scratch with semantic HTML, CSS-generated creator visuals, and seamless duplicated reel animations.

## Run locally

Open `index.html` directly, or serve the directory with any static file server:

```bash
python3 -m http.server 4173
```
